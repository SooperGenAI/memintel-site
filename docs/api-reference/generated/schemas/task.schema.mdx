---
id: task
title: "Task"
description: "Canonical task model. Represents both the DB row and the API response."
sidebar_label: "Task"
hide_title: true
hide_table_of_contents: true
schema: true
sample: {"task_id":"string","intent":"string","concept_id":"string","concept_version":"string","condition_id":"string","condition_version":"string","action_id":"string","action_version":"string","entity_scope":"string","delivery":{"type":"webhook","endpoint":"string","channel":"string","workflow_id":"string"},"status":"active","created_at":"2024-07-29T15:51:28.071Z","last_triggered_at":"2024-07-29T15:51:28.071Z","context_version":"string","context_warning":"string","guardrails_version":"string"}
custom_edit_url: null
---

import Schema from "@theme/Schema";
import Heading from "@theme/Heading";

<Heading
  as={"h1"}
  className={"openapi__heading"}
  children={"Task"}
>
</Heading>



Canonical task model. Represents both the DB row and the API response.

Lifecycle
─────────
Creation  → task_id is None; populated by TaskStore.create() from DB default.
Read      → all fields populated from the DB row.
Update    → TaskStore.update() enforces IMMUTABLE_TASK_FIELDS and optimistic
            locking via the version field.
Delete    → status transitions to 'deleted'; the row is never hard-deleted.
Preview   → status='preview'; TaskStore.create() must reject these rows.

Internal fields (excluded from JSON serialisation)
──────────────────────────────────────────────────
version    — optimistic locking counter. Read before UPDATE, increment in
             WHERE clause. Never exposed to API callers.
updated_at — DB-managed timestamp. Not in the API spec; internal only.

<Schema
  {...require("./task.Schema.json")}
>
  
</Schema>
            