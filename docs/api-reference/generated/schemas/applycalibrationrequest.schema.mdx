---
id: applycalibrationrequest
title: "ApplyCalibrationRequest"
description: "Applies a calibration recommendation as a new immutable condition version. rebind_tasks is NOT a field on this request — rebinding is always a separate explicit caller action via PATCH /tasks/{id}. threshold is NOT a field — use calibration_token from the calibrate response; params are resolved server-side from the token."
sidebar_label: "ApplyCalibrationRequest"
hide_title: true
hide_table_of_contents: true
schema: true
sample: {"calibration_token":"string","new_version":"string"}
custom_edit_url: null
---
import Schema from "@theme/Schema";
import Heading from "@theme/Heading";

<Heading
  as={"h1"}
  className={"openapi__heading"}
  children={"ApplyCalibrationRequest"}
>
</Heading>



Applies a calibration recommendation as a new immutable condition version. rebind_tasks is NOT a field on this request — rebinding is always a separate explicit caller action via PATCH /tasks/\{id\}. threshold is NOT a field — use calibration_token from the calibrate response; params are resolved server-side from the token.


<Schema
  {...require("./applycalibrationrequest.Schema.json")}
>
  
</Schema>
            





