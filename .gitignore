# Build output
/build
/.docusaurus

# Dependencies
/node_modules

# Local env files
.env
.env.local

# macOS
.DS_Store

# Editor
.idea
.vscode

# Note: docs/api-reference/generated/ is intentionally COMMITTED
# so the site builds in read-only CI environments without the gen step.
